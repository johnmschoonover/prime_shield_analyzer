# Generated by https://www.toptal.com/developers/gitignore/api/rust
# Edit at https://www.toptal.com/developers/gitignore?templates=rust

### Rust ###
# Standard target directory for compiled artifacts
/target/

# Depending on your project, you may want to remove these files
# if you want to be able to rebuild from scratch with just Cargo.toml.
# Cargo.lock is usually committed for applications but not for libraries.
# For this application, we are committing Cargo.lock.
# Cargo.lock

# These are always ignored, just don't expect them to disappear from 'git status'.
# It is necessary to remove the corresponding entry in `~/.cargo/config.toml`
# if you ever want to commit them.
# .cargo/
# .cargo/config.toml # If you don't want to commit .cargo/config.toml for custom linker settings.
# For this project, we are committing .cargo/config.toml.

# Rust-specific build/dependency files
**/*.rs.bk
**/*.wasm
**/*.d

# Build artifacts for C/C++ FFI
# *.o
# *.so
# *.dylib
# *.a

# IDEs and other tools
.DS_Store
.env
.env.local
.history/
.idea/
.vscode/
*.bak
*.log
*.log.*
*.profdata
*.swo
*.swp
*.tmp
*.sublime-project
*.sublime-workspace
Thumbs.db
build/
coverage/
dist/
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.lock-wscript
.wafpickle-*
.vs/
.classpath
.project
.settings/
*.iml
*.ipr
*.iws

# Our application-specific output
# Our application-specific output
/results/

# pre-commit cache and virtual environment
.pre-commit/
.pre-commit-cache/
venv/
.venv/
/html/ # If any other HTML output is generated outside results/

# Local development environments
.venv/
.envrc
.direnv/
